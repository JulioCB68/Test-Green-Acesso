import styled from "styled-components";

export const Container = styled.div`
	position: absolute;
	top: 0;
	right: 0;
	padding: 0.5rem 1rem;
	background: #444b5c;
	border-radius: 0.25rem;
	opacity: 1;
`;
